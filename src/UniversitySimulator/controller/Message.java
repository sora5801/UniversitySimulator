package UniversitySimulator.controller;

/**
 * This is the main message interface. This is the interface
 * that passes what the view wants to the controller for the controller to execute the command.
 */
public interface Message {
    String getName();
}
